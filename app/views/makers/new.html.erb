<h1>Add a New Maker</h1>

<%= form_with(model: @maker, local: true) do |f| %>
  <%= f.label :brand %>
  <%= f.text_field :brand %>
  <br>
  <%= f.submit %>
<% end %>

<br>

<%= link_to 'Back', makers_path %>
